export const PANEL_W = 280;

export const LONG_PRESS_MS = 600;
export const LONG_PRESS_MOVE_PX = 8;

/** Horizontal inset inside matrix pills; keep on inner wrapper so % / flex positioning stays exact. */
export const MATRIX_PILL_INSET_X = "px-1";

/** Matrix drag band fill (`#7BA3B5` primary). */
export const MATRIX_DRAG_HIGHLIGHT_OVER = "rgba(123, 163, 181, 0.28)";
export const MATRIX_DRAG_HIGHLIGHT_IDLE = "rgba(123, 163, 181, 0.18)";
